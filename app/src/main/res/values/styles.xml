<resources>

    <!-- Base application theme. -->
    <style name="MMBAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/md_pink_600</item>
        <item name="colorPrimaryDark">@color/md_pink_600</item>
        <item name="colorAccent">@color/md_pink_600</item>
    </style>

</resources>
